/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "utfs.io",
            "res.cloudinary.com" 
        ],
        // hostname: [
        //     "res.cloudinary.com"
        // ]
    }
};

export default nextConfig;
 